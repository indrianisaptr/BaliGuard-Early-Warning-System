# Core dashboard
streamlit
plotly

# Data processing
pandas
numpy
pyarrow

# Machine Learning (dipakai src/repositories & src/services untuk load model/inference)
scikit-learn
joblib

# Image handling
Pillow

# Backend / storage
supabase

# LLM
groq

# HTTP client — dipakai src/services/llm_service.py (import top-level)
requests

# Date arithmetic — dipakai src/services/forecast.py (import top-level)
python-dateutil
